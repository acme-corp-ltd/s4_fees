{% extends 'base.html.twig' %}

{% block title %}Extract{% endblock %}

{% macro datarow(item) %}
    <tr>
        <td>{{ item.category }}</td>
        <td>{{ item.limLow }}</td>
        <td>{{ item.limTop }}</td>
        <td>{{ item.fee }}</td>
    </tr>
{% endmacro %}

{% block body %}
    <div>
        <table>
            <tr>
                <th>Category</th>
                <th>LimLow</th>
                <th>LimTop</th>
                <th>Fee</th>
            </tr>
            {% if datalist is defined and datalist %}
                {% for item in datalist %}
                    {{ _self.datarow(item) }}
                {% endfor %}
            {% endif %}
            {% if dataitem is defined and dataitem %}
                {{ _self.datarow(dataitem) }}
            {% endif %}
        </table>
    </div>
{% endblock %}
